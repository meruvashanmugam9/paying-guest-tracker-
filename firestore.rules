rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 1. Global Safety Net (Default Deny Catch-all)
    match /{document=**} {
      allow read, write: if false;
    }

    // Common global helper primitives
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isValidId(id) {
      return id is string && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$');
    }

    // 2. User root documents
    match /users/{userId} {
      allow read, write: if isOwner(userId);
    }

    // 3. User subcollections (Flat Rules)
    match /users/{userId}/rooms/{roomId} {
      allow list: if isOwner(userId);
      allow get, create, update, delete: if isOwner(userId) && isValidId(roomId);
    }

    match /users/{userId}/guests/{guestId} {
      allow list: if isOwner(userId);
      allow get, create, update, delete: if isOwner(userId) && isValidId(guestId);
    }

    match /users/{userId}/payments/{paymentId} {
      allow list: if isOwner(userId);
      allow get, create, update, delete: if isOwner(userId) && isValidId(paymentId);
    }

    match /users/{userId}/deletedGuests/{deletedGuestId} {
      allow list: if isOwner(userId);
      allow get, create, update, delete: if isOwner(userId) && isValidId(deletedGuestId);
    }
  }
}
